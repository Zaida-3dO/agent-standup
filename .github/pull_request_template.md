<!--
If this PR delivers a board row, name its id below — a full UUID, verbatim. That is the only
signal scripts/reconcile-shipped-rows.mjs has for finding rows that shipped but never got closed
(CLAUDE.md, "Commits"). Delete this line if there is no row.
-->

Row: <uuid>

## What changed and why
