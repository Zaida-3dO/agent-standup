// `/fleet` — M10 T16: every live assignment, grouped by liveness and
// filterable by machine and agent, plus sweep and takeover recovery.
import { Fleet } from "@/components/fleet/Fleet";

export default function FleetPage() {
  return <Fleet />;
}
