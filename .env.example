# The database this server owns. It belongs to the SERVER, not to its
# clients: a client on another machine talks to the API and never opens a
# connection of its own. That is not a preference about tidiness — every
# rule this product enforces (a merge needing an approving review at tip, a
# completion needing a summary) is application code in the service layer,
# and a direct connection reaches the tables without passing through any of
# it. See STANDUP_TOKENS below for the path a remote client uses instead.
DATABASE_URL=postgres://standup:standup@localhost:5433/standup
# Per-machine bearer tokens — `machine:token`, comma-separated. Every call
# to the API and to the MCP mount presents one, and the machine it resolves
# to is what makes an actor header worth recording rather than merely
# accepting. Give each machine its own so one can be revoked without
# rotating the rest.
#
# There is no default and no development fallback: with this unset the
# server refuses every authenticated call. A gate that switched itself off
# when its configuration was missing would be open exactly when a
# deployment had gone wrong.
#
# Generate one per machine with something like:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
# STANDUP_TOKENS=laptop:REPLACE_ME,desktop:REPLACE_ME_TOO
#
# The matching client-side value. A client sends this one token; the server
# matches it against its own list. Never a flag — a credential typed as an
# argument lands in shell history and the process list.
# STANDUP_TOKEN=REPLACE_ME
# Development and CI only — the disposable database the migration drift
# check (`npm run db:check-drift`) drops and rebuilds. Never points at
# anything worth keeping.
# SHADOW_DATABASE_URL=postgres://standup:standup@localhost:5433/standup_shadow
# Boot sequence — how long scripts/entrypoint.mjs waits for Postgres to
# accept a query before giving up, and how often it retries. Commented out
# here because the default (below) already covers a slow cold boot; unset
# uses it. If set, must be a whole or decimal number of seconds > 0 — an
# empty value is treated the same as an invalid one, not as "unset".
# DB_WAIT_TIMEOUT_SECONDS=180
# DB_WAIT_INTERVAL_SECONDS=2
# The liveness sweep's schedule — read by scripts/sweep-schedule.mjs, which
# runs as its own process beside the app (docker-compose.prod.yml's
# `sweep-scheduler`) because a schedule belongs to the deployment rather
# than to a per-replica timer inside the bundle. STANDUP_URL is where the
# app answers and has no default; the two durations follow the same rule as
# the boot knobs above, where an empty value is invalid rather than unset.
# STANDUP_URL=http://localhost:3000
# SWEEP_INTERVAL_SECONDS=300
# SWEEP_TIMEOUT_SECONDS=60
# Two windows that widen a write surface, both fail-closed and both opened
# from the deployment layer rather than through the API — which is the whole
# point of them being environment variables: nothing reachable over HTTP, MCP
# or the command line can turn either on for itself. Each announces itself in
# the startup log while open, because the realistic failure is opening one for
# a single job, being interrupted, and leaving it open. Only the exact string
# `true` enables either; every other value, including `1`, `yes` and `TRUE`,
# leaves it shut.
#
# ENABLE_BACKFILL — the bulk-import surface, which bypasses the state machine.
# ENABLE_BACKFILL=true
#
# ENABLE_HISTORICAL_VERIFICATION — lets an item merge on a recorded inspection
# of already-shipped code instead of an approving review, for work that
# finished before this installation existed. It never substitutes for a
# person's approval on an item whose merge authority requires one.
# ENABLE_HISTORICAL_VERIFICATION=true
#
# Everything else the application reads is a database setting with a
# code-side default, not an environment variable — configure it via
# /settings or `standup config set` once the app is running. See
# docs/plans/SCHEMA.md §17 for the full registry and why the line sits here.
